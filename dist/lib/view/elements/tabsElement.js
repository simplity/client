import { BaseElement } from './baseElement';
export class TabsElement extends BaseElement {
    constructor(pc, fc, tabs, maxWidth) {
        super(pc, fc, tabs, 'tabs', maxWidth);
    }
}
//# sourceMappingURL=tabsElement.js.map